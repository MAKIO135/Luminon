int track = 0;
int trackStart=0;
int trackTime=0;

void timeline(){
	trackTime = millis()-trackStart;

	switch (track){
		case 0 :
			// if(trackTime < 40000) displayMode = 0;
			// else if(trackTime < ) displayMode = 4;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
		case 1 :
			// if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
		case 2 :
			// if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
		case 3 :
			// if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
		case 4 :
			// if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
		case 5 :
			// if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
			// else if(trackTime < ) displayMode = ;
		break;
	}
}